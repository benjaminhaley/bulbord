services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-campy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-campy}
      POSTGRES_DB: ${POSTGRES_DB:-campy_dev}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-campy}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
